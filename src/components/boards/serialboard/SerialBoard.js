import React from 'react'

export default function SerialBoard() {
    return (
        <div style={{ display: 'flex', flexDirection: 'column', maxHeight: '100%' }}>
            <h3>Serial Board</h3>
            <img style={{ maxHeight: '100%', maxWidth: '100%' }} src={'images/serialboard.jpg'} alt="alarm panel pic" />
        </div>
    )
}
